# React_Projects_1.ToDoList-

1. Create project

   > > npm create vite@latest
   > > Project name: ... vite-project
   > > Select a framework: » React
   > > Select a variant: » JavaScript
   > > cd vite-project
   > > npm install
   > > npm run dev

2. install bootstrap
   npm i bootstrap@5.3.2
3. import in main.jsc
